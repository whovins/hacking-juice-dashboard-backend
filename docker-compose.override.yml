services:
  api:
    command: ["bash","-lc","alembic upgrade head || true && uvicorn app.main:create_app --factory --host 0.0.0.0 --port 8000 --reload"]
  worker:
    command: ["bash","-lc","python -m app.workers.worker"]
